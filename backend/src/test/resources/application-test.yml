spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration
  docker:
    compose:
      enabled: false
  rabbitmq:
    listener:
      simple:
        auto-startup: false

logging:
  level:
    com.facecheck: DEBUG
